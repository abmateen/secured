#include <stdio.h>
 


/* Get process info by supplying process id */

PROCESS_INFO getProcessInfobyPid ( int pid );

/* Manage a queue for Processes to manage */

void add_to_queue(PROCESS_INFO*, PROCESS_QUEUE*);
